import InfoPage from "@/components/InfoPage";

export const metadata = { title: "Devoluciones — LUNA" };

export default function DevolucionesPage() {
  return (
    <InfoPage eyebrow="Ayuda" title="Devoluciones">
      <p>Si no estás satisfecho, puedes devolver tu producto dentro de los 30 días posteriores a recibirlo.</p>
    </InfoPage>
  );
}
